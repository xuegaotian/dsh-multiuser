# Copy to /etc/dsh-multiuser/gateway.env and chmod 0600.
DEEPSEEK_API_KEY=
# Optional private-compatible endpoint.
DEEPSEEK_BASE_URL=
# Gateway SSO public keys are configured as CLI arguments in the service unit.
# Keep public key files readable by dsh-multiuser only under /etc/dsh-multiuser/keys/.
