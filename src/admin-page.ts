/** Render the administrator's global configuration and Runtime control page. */
export function adminPageHtml(): string {
  return finalizeAdminPage(`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>DSH 管理控制台</title><style>
:root{color-scheme:dark;--bg:#0b0c0a;--panel:#111310;--line:#2b312b;--muted:#8b968c;--text:#edf3ed;--signal:#c6ff4a;--danger:#ff3b30}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:13px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace}button,input,select{font:inherit;font-size:12px}button{cursor:pointer;border:1px solid var(--line);background:#151814;color:var(--text);padding:8px 12px}button:hover:not(:disabled){border-color:var(--signal);color:var(--signal)}button:disabled{cursor:not-allowed;opacity:.5}button.primary{background:var(--signal);border-color:var(--signal);color:#0b0c0a;font-weight:700}button.danger-button{color:var(--danger);border-color:#71312e;background:transparent}input,select{width:100%;color:var(--text);background:#090a09;border:1px solid var(--line);padding:10px;outline:0}input:focus,select:focus{border-color:var(--signal)}a{color:var(--muted);text-decoration:none}.app-shell{display:grid;grid-template-columns:232px minmax(0,1fr);min-height:100vh}.rail{position:sticky;top:0;height:100vh;border-right:1px solid var(--line);padding:26px 18px;background:#090a09}.brand{display:flex;gap:10px;padding:0 10px 30px;border-bottom:1px solid var(--line)}.brand-mark{display:grid;place-items:center;width:25px;height:25px;border:1px solid var(--signal);color:var(--signal)}.brand-sub{display:block;color:var(--muted);font-size:10px}.nav{display:grid;gap:4px;padding-top:24px}.nav a{padding:10px;border-left:2px solid transparent}.nav a:hover,.nav a.active{color:var(--signal);background:#121610;border-left-color:var(--signal)}.content{min-width:0}.topbar{display:flex;justify-content:space-between;border-bottom:1px solid var(--line);padding:18px 4vw;color:var(--muted);font-size:11px}.topbar-status{color:var(--signal)}main{width:min(1240px,100%);margin:auto;padding:42px 4vw 80px}h1{font-size:32px;font-weight:500}h2{font-size:16px;font-weight:500}h3{font-size:13px;font-weight:500}.intro,.section-note,.resource-meta{color:var(--muted)}section{margin-top:48px;padding-top:25px;border-top:1px solid var(--line)}.panel,.table-wrap,.resource-list{border:1px solid var(--line);background:var(--panel)}.config-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;padding:24px}.field{display:block;margin-bottom:16px;color:var(--muted)}.field input,.field select{margin-top:7px}.table-wrap{overflow:auto}table{width:100%;min-width:760px;border-collapse:collapse}th,td{padding:14px 16px;text-align:left;border-bottom:1px solid var(--line);white-space:nowrap}th{color:var(--muted);font-size:10px;font-weight:500}tr:last-child td{border-bottom:0}.row-actions,.resource-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.resource-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:20px;align-items:center;padding:16px 18px;border-bottom:1px solid var(--line)}.resource-row:last-child{border-bottom:0}.resource-name{margin:0 0 5px;font-weight:600}.empty-resource{padding:18px;color:var(--muted)}.session-row{display:flex;gap:14px;padding:13px 16px;border-bottom:1px solid var(--line)}.session-name{flex:1;overflow:hidden;text-overflow:ellipsis}dialog{width:min(620px,calc(100% - 32px));border:1px solid var(--line);background:var(--panel);color:var(--text);padding:24px}dialog::backdrop{background:#000b}.dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:22px}#status{position:fixed;right:24px;bottom:24px;padding:13px 16px;border:1px solid var(--signal);background:#16200f;color:var(--signal)}#status.error{border-color:var(--danger);background:#28110f;color:#ff8b85}@media(max-width:800px){.app-shell{display:block}.rail{position:static;width:auto;height:auto;padding:16px 20px}.nav{display:flex;overflow:auto;padding-top:10px}.topbar{display:block}.config-grid{grid-template-columns:1fr}.resource-row{grid-template-columns:1fr}}
</style></head><body><div class="app-shell"><aside class="rail"><div class="brand"><span class="brand-mark">D</span><div>DSH / OPS<span class="brand-sub">ADMIN CONSOLE</span></div></div><nav class="nav"><a class="active" href="#overview">总览</a><a href="#model">公共模型</a><a href="#skills">公共 Skill</a><a href="#plugins">公共插件</a><a href="#users">用户与会话</a></nav></aside><div class="content"><div class="topbar"><span>DSH MULTI-USER / CONTROL PLANE</span><span class="topbar-status">管理员权限已验证</span></div><main><h1>管理控制台</h1><p class="intro">集中维护所有用户 Runtime 继承的公共资源，并查看实例状态。</p><div id="overview"><strong id="metric-users">-</strong> 用户　<strong id="metric-running">-</strong> 运行中 Runtime　<strong id="metric-enabled">-</strong> 已启用账号</div><section id="model"><h2>公共模型</h2><p class="section-note">提供商列表展示当前可用的公共模型。内置 DeepSeek 由 DSH Profile 管理，自定义提供商由管理员维护。</p><div class="panel config-grid"><div><label class="field">DeepSeek API 密钥<input id="api-key" type="password" autocomplete="new-password"></label><label class="field">兼容接口地址<input id="base-url" type="url" placeholder="https://api.example.com"></label><button id="save-model" class="primary">保存 DeepSeek 配置</button></div><div><div id="model-state" class="resource-meta">正在读取配置...</div><div style="display:flex;justify-content:space-between;align-items:center;margin-top:22px"><h3 style="margin:0">已配置提供商</h3><button id="open-provider" class="primary">添加提供商</button></div><div id="providers-list" class="resource-list"></div></div></div></section><section id="skills"><h2>公共 Skill</h2><div id="skills-list" class="resource-list"></div></section><section id="plugins"><h2>公共插件</h2><div id="plugins-list" class="resource-list"></div></section><section id="users"><div style="display:flex;justify-content:space-between"><h2>普通用户</h2><button id="refresh">刷新数据</button></div><div class="panel config-grid"><input id="new-username" placeholder="用户名"><input id="new-display-name" placeholder="显示名称"><input id="new-password" type="password" placeholder="初始密码（至少 12 个字符）"><button id="add-user" class="primary">添加普通用户</button></div><div class="table-wrap"><table><thead><tr><th>用户</th><th>账号</th><th>Runtime</th><th>操作</th></tr></thead><tbody id="users-body"></tbody></table></div><h3>已选用户的会话</h3><div id="session-actions" class="panel"></div><div id="session-detail-actions"></div><pre id="sessions" hidden></pre></section></main></div></div><p id="status" hidden></p><dialog id="provider-dialog"><form method="dialog"><h2>添加自定义提供商</h2><label class="field">提供商类型<select id="provider-kind"><option value="custom">自定义提供商</option></select></label><label class="field">提供商 ID<input id="provider-id" placeholder="例如 acme-gateway"></label><label class="field">显示名称<input id="provider-name" placeholder="可选"></label><label class="field">协议<select id="provider-api"><option value="openai-completions">OpenAI Chat Completions</option><option value="openai-responses">OpenAI Responses</option><option value="anthropic-messages">Anthropic Messages</option></select></label><label class="field">API 地址<input id="provider-url" placeholder="https://host/v1"></label><label class="field">API 密钥<input id="provider-key" type="password"></label><label class="field">模型 ID<input id="provider-model" placeholder="多个模型用逗号分隔"></label><div class="dialog-actions"><button value="cancel">取消</button><button id="save-provider" value="default" class="primary">保存提供商</button></div></form></dialog><script>
const q=s=>document.querySelector(s),users=q('#users-body'),sessions=q('#sessions'),sessionActions=q('#session-actions'),sessionDetailActions=q('#session-detail-actions'),status=q('#status'),providersList=q('#providers-list'),dialog=q('#provider-dialog');let providers=[],noticeTimer
async function body(response){const result = await response,data=await result.json();if(!result.ok)throw new Error(data.error||'请求失败');return data}
function showStatus(message,error=false){clearTimeout(noticeTimer);status.textContent=message;status.className=error?'error':'';status.hidden=false;noticeTimer=setTimeout(()=>{status.hidden=true},6000)}
function clearProviderForm(){for(const id of ['provider-id','provider-name','provider-url','provider-key','provider-model'])q('#'+id).value='';q('#provider-api').value='openai-completions'}
function renderProviders(){providersList.replaceChildren(...providers.map(provider=>{const row=document.createElement('div'),info=document.createElement('div'),title=document.createElement('p'),meta=document.createElement('p'),actions=document.createElement('div');row.className='resource-row';title.className='resource-name';meta.className='resource-meta';title.textContent=provider.displayName||provider.provider;meta.textContent=provider.provider+' · '+provider.api+' · '+provider.baseURL+' · 模型：'+provider.models.map(model=>model.id).join(', ')+' · API Key '+(provider.apiKeyConfigured?'已配置':'未配置');actions.className='resource-actions';info.append(title,meta);const edit=document.createElement('button');edit.textContent='修改';edit.onclick=()=>{if(provider.builtIn){q('#base-url').focus();showStatus('内置 DeepSeek 的地址请在左侧修改，API Key 可直接重新填写。');return}q('#provider-id').value=provider.provider;q('#provider-name').value=provider.displayName||'';q('#provider-api').value=provider.api;q('#provider-url').value=provider.baseURL;q('#provider-model').value=provider.models.map(model=>model.id).join(', ');q('#provider-key').value='';q('#save-provider').textContent='更新提供商';dialog.showModal()};actions.append(edit);const remove=document.createElement('button');remove.className='danger-button';remove.textContent=provider.builtIn?'不可删除':'删除';remove.disabled=provider.builtIn;remove.onclick=()=>{providers=providers.filter(item=>item.provider!==provider.provider);renderProviders();saveModel().catch(error=>showStatus(error.message,true))};actions.append(remove);row.append(info,actions);return row}))}
async function readModel(){const data=await body(fetch('/admin/global-config'));q('#model-state').textContent=data.apiKeyConfigured?'DeepSeek API 密钥已配置':'尚未配置 DeepSeek API 密钥';q('#base-url').value=data.baseUrl||'';providers=[{provider:'deepseek-official',displayName:'DeepSeek',api:'DeepSeek API',baseURL:data.baseUrl||'https://api.deepseek.com',models:[{id:'deepseek-v4-flash'}],apiKeyConfigured:Boolean(data.apiKeyConfigured),builtIn:true},...(data.providers||[])];renderProviders()}
async function saveModel(){await body(fetch('/admin/global-config',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({apiKey:q('#api-key').value,baseUrl:q('#base-url').value,providers:providers.filter(item=>!item.builtIn).map(item=>({...item,apiKey:item.apiKey||''}))})}));q('#api-key').value='';showStatus('公共模型配置已保存，现有 Runtime 已停止。');await readModel()}
q('#open-provider').onclick=()=>{clearProviderForm();q('#save-provider').textContent='保存提供商';dialog.showModal()};q('#save-provider').onclick=async event=>{event.preventDefault();const id=q('#provider-id').value.trim(),key=q('#provider-key').value.trim(),index=providers.findIndex(item=>item.provider===id);if(!id||(!key&&index<0)||!q('#provider-url').value.trim()||!q('#provider-model').value.trim()){showStatus('请填写提供商 ID、地址、密钥和模型 ID',true);return}const value={provider:id,displayName:q('#provider-name').value.trim()||undefined,api:q('#provider-api').value,baseURL:q('#provider-url').value.trim(),models:q('#provider-model').value.split(',').map(id=>({id:id.trim()})).filter(item=>item.id),...(key?{apiKey:key}:{apiKey:providers[index].apiKey}),apiKeyConfigured:true};if(index<0)providers.push(value);else providers[index]=value;renderProviders();try{await saveModel();dialog.close();clearProviderForm()}catch(error){showStatus(error.message,true)}};q('#save-model').onclick=()=>saveModel().catch(error=>showStatus(error.message,true))
async function load(){const data=await body(fetch('/admin/users')),rows=data.users;q('#metric-users').textContent=rows.length;q('#metric-running').textContent=rows.filter(row=>row.runtime.status==='RUNNING').length;q('#metric-enabled').textContent=rows.filter(row=>row.user.enabled).length;users.replaceChildren(...rows.map(row=>{const tr=document.createElement('tr'),a=tr.insertCell(),b=tr.insertCell(),c=tr.insertCell(),d=tr.insertCell();a.textContent=row.user.displayName;b.textContent=row.user.username;c.textContent=row.runtime.status;const runtime=document.createElement('button');runtime.textContent=row.runtime.status==='RUNNING'?'停止 Runtime':'启动 Runtime';runtime.onclick=()=>{runtime.disabled=true;const action=row.runtime.status==='RUNNING'?'stop':'start';return body(fetch('/admin/runtime/'+encodeURIComponent(row.user.id)+'/'+action,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(action==='stop'?{confirm:true}:{})})).then(load).catch(error=>showStatus(error.message,true))};const view=document.createElement('button');view.textContent='查看会话';view.onclick=()=>readSessions(row.user.id,'list',{});d.append(runtime,view);if(row.user.role==='user'){const remove=document.createElement('button');remove.className='danger-button';remove.textContent='删除用户';remove.onclick=()=>{if(confirm('确定删除用户 '+row.user.username+'？'))body(fetch('/admin/users/'+encodeURIComponent(row.user.id),{method:'DELETE'})).then(load).catch(error=>showStatus(error.message,true))};d.append(remove)}return tr}))}
async function readSessions(userId,method,payload){const data=await body(fetch('/admin/sessions/'+encodeURIComponent(userId)+'/'+method,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}));if(method==='list'){const items=data.result?.value?.items||[];sessionActions.replaceChildren(...items.map(item=>{const row=document.createElement('div'),name=document.createElement('span'),open=document.createElement('button');row.className='session-row';name.className='session-name';name.textContent=item.displayTitle||item.title||item.sessionId;open.textContent='查看详情';open.onclick=()=>readSessions(userId,'history',{sessionId:item.sessionId,maxMessages:100});row.append(name,open);return row}));sessions.hidden=true;return}sessionDetailActions.textContent='';const close=document.createElement('button');close.textContent='关闭会话详情';close.onclick=()=>{sessions.hidden=true};sessionDetailActions.append(close);sessions.textContent=JSON.stringify(data,null,2);sessions.hidden=false}
async function loadSkills(){const data=await body(fetch('/admin/public-skills')),root=q('#skills-list');root.replaceChildren(...data.skills.map(skill=>{const row=document.createElement('div');row.className='resource-row';row.textContent=skill.name+'（'+skill.files.length+' 个文件）';root.append(row);return row}))}async function loadPlugins(){const data=await body(fetch('/admin/public-plugins')),root=q('#plugins-list');root.replaceChildren(...data.bundles.map(name=>{const row=document.createElement('div');row.className='resource-row';row.textContent=name;root.append(row);return row}))}
q('#add-user').onclick=()=>body(fetch('/admin/users',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:q('#new-username').value,displayName:q('#new-display-name').value,password:q('#new-password').value})})).then(()=>{showStatus('普通用户已添加');return load()}).catch(error=>showStatus(error.message,true));q('#refresh').onclick=()=>load().catch(error=>showStatus(error.message,true));Promise.all([load(),readModel(),loadSkills(),loadPlugins()]).catch(error=>showStatus(error.message,true))
</script></body></html>`)
}

function finalizeAdminPage(html: string): string {
  const modelScript = String.raw`async function readModel(){const data=await body(fetch('/admin/global-config'));q('#model-state').textContent=data.apiKeyConfigured?'DeepSeek API 密钥已配置':'尚未配置 DeepSeek API 密钥';providers=[...(data.apiKeyConfigured?[{provider:'deepseek-official',displayName:'DeepSeek',api:'DeepSeek API',baseURL:data.baseUrl||'https://api.deepseek.com',models:[{id:'deepseek-v4-flash'}],apiKeyConfigured:true,builtIn:true}]:[]),...(data.providers||[])];renderProviders()}
async function saveModel(apiKey='',baseUrl=''){const deepseek=providers.find(item=>item.builtIn);await body(fetch('/admin/global-config',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({apiKey,baseUrl:baseUrl||deepseek?.baseURL||'',providers:providers.filter(item=>!item.builtIn).map(item=>({...item,apiKey:item.apiKey||''}))})}));showStatus('公共模型配置已保存，现有 Runtime 已停止。');await readModel()}
function toggleProviderFields(){const deepseek=q('#provider-kind').value==='deepseek';for(const id of ['provider-id-field','provider-name-field','provider-api-field','provider-model-field'])q('#'+id).hidden=deepseek;q('#provider-url').placeholder=deepseek?'https://api.deepseek.com':'https://host/v1'}
q('#open-provider').onclick=()=>{clearProviderForm();q('#provider-kind').value=providers.some(item=>item.builtIn)?'custom':'deepseek';toggleProviderFields();q('#save-provider').textContent='保存提供商';dialog.showModal()};q('#provider-kind').onchange=toggleProviderFields;q('#save-provider').onclick=async event=>{event.preventDefault();const kind=q('#provider-kind').value;if(kind==='deepseek'){const key=q('#provider-key').value.trim(),baseUrl=q('#provider-url').value.trim();if((!key&&!providers.some(item=>item.builtIn))||!baseUrl){showStatus('请填写 DeepSeek API 密钥和地址',true);return}try{await saveModel(key,baseUrl);dialog.close();clearProviderForm()}catch(error){showStatus(error.message,true)}return}const id=q('#provider-id').value.trim(),key=q('#provider-key').value.trim(),index=providers.findIndex(item=>item.provider===id);if(!id||(!key&&index<0)||!q('#provider-url').value.trim()||!q('#provider-model').value.trim()){showStatus('请填写提供商 ID、地址、密钥和模型 ID',true);return}const value={provider:id,displayName:q('#provider-name').value.trim()||undefined,api:q('#provider-api').value,baseURL:q('#provider-url').value.trim(),models:q('#provider-model').value.split(',').map(id=>({id:id.trim()})).filter(item=>item.id),...(key?{apiKey:key}:{apiKey:providers[index].apiKey}),apiKeyConfigured:true};if(index<0)providers.push(value);else providers[index]=value;try{await saveModel('',providers.find(item=>item.builtIn)?.baseURL||'');dialog.close();clearProviderForm()}catch(error){showStatus(error.message,true)}}
`
  const resourceScript = String.raw`function base64(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=()=>reject(reader.error||new Error('无法读取文件'));reader.onload=()=>resolve(String(reader.result).split(',',2)[1]||'');reader.readAsDataURL(file)})}
function emptyResource(text){return Object.assign(document.createElement('div'),{className:'empty-resource',textContent:text})}
function runButton(button,pending,action){button.disabled=true;const original=button.textContent;button.textContent=pending;return action().finally(()=>{button.disabled=false;button.textContent=original})}
function toggleSkillMode(){const folder=q('#skill-mode').value==='folder';q('#skill-manual-fields').hidden=folder;q('#skill-folder-fields').hidden=!folder}
function openSkill(skill){q('#skill-dialog-title').textContent=skill?'修改公共 Skill':'添加公共 Skill';q('#skill-mode').value='manual';q('#skill-mode').disabled=Boolean(skill);q('#skill-name').value=skill?.name||'';q('#skill-name').disabled=Boolean(skill);q('#skill-content').value=skill?.content||'';q('#skill-folder').value='';toggleSkillMode();q('#skill-dialog').showModal()}
async function importSkill(){const entries=Array.from(q('#skill-folder').files);if(entries.length===0)throw new Error('请选择包含 SKILL.md 的完整 Skill 文件夹');const roots=new Set(entries.map(file=>(file.webkitRelativePath||file.name).split('/',1)[0]));if(roots.size!==1)throw new Error('请选择一个 Skill 文件夹');const name=[...roots][0],files=await Promise.all(entries.map(async file=>{const full=file.webkitRelativePath||file.name;return{path:full.slice(name.length+1),content:await base64(file)}}));await body(fetch('/admin/public-skills/import',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({name,files})}));return name}
async function loadSkills(){const data=await body(fetch('/admin/public-skills')),root=q('#skills-list');if(data.skills.length===0){root.replaceChildren(emptyResource('暂无公共 Skill'));return}root.replaceChildren(...data.skills.map(skill=>{const row=document.createElement('div'),info=document.createElement('div'),title=document.createElement('p'),meta=document.createElement('p'),actions=document.createElement('div'),edit=document.createElement('button'),remove=document.createElement('button');row.className='resource-row';title.className='resource-name';meta.className='resource-meta';actions.className='resource-actions';title.textContent=skill.name;meta.textContent=skill.files.length+' 个文件：'+skill.files.join('、');edit.textContent='修改';edit.onclick=()=>openSkill(skill);remove.className='danger-button';remove.textContent='删除';remove.onclick=()=>{if(!confirm('删除公共 Skill「'+skill.name+'」？'))return;body(fetch('/admin/public-skills/'+encodeURIComponent(skill.name),{method:'DELETE'})).then(async()=>{showStatus('已删除公共 Skill：'+skill.name);await loadSkills()}).catch(error=>showStatus(error.message,true))};info.append(title,meta);actions.append(edit,remove);row.append(info,actions);return row}))}
const requiredPlugins=new Set(['@deepseek-ai/dsh-base','@deepseek-ai/dsh-web-app','@dsh-multiuser/user-runtime-bundle']);async function loadPlugins(){const data=await body(fetch('/admin/public-plugins')),root=q('#plugins-list');if(data.bundles.length===0){root.replaceChildren(emptyResource('暂无公共插件'));return}root.replaceChildren(...data.bundles.map(name=>{const row=document.createElement('div'),info=document.createElement('div'),title=document.createElement('p'),meta=document.createElement('p'),actions=document.createElement('div');row.className='resource-row';title.className='resource-name';meta.className='resource-meta';actions.className='resource-actions';title.textContent=name;meta.textContent=requiredPlugins.has(name)?'系统必需插件':'公共插件';info.append(title,meta);if(requiredPlugins.has(name)){actions.append(Object.assign(document.createElement('span'),{className:'resource-meta',textContent:'不可删除'}))}else{const remove=document.createElement('button');remove.className='danger-button';remove.textContent='删除';remove.onclick=()=>{if(!confirm('删除公共插件「'+name+'」？'))return;body(fetch('/admin/public-plugins',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'remove',name})})).then(async()=>{showStatus('已删除公共插件：'+name);await loadPlugins()}).catch(error=>showStatus(error.message,true))};actions.append(remove)}row.append(info,actions);return row}))}
q('#open-skill').onclick=()=>openSkill();q('#skill-mode').onchange=toggleSkillMode;q('#skill-dialog').addEventListener('close',()=>{q('#skill-mode').disabled=false;q('#skill-name').disabled=false});q('#save-skill').onclick=event=>{event.preventDefault();const button=q('#save-skill');runButton(button,'正在保存...',async()=>{const name=q('#skill-mode').value==='folder'?await importSkill():(await body(fetch('/admin/public-skills',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({name:q('#skill-name').value,content:q('#skill-content').value})})),q('#skill-name').value);q('#skill-dialog').close();showStatus('已保存公共 Skill：'+name+'。已运行 Runtime 请重启。');await loadSkills()}).catch(error=>showStatus(error.message,true))};q('#open-plugin').onclick=()=>{q('#plugin-spec').value='';q('#plugin-dialog').showModal()};q('#save-plugin').onclick=event=>{event.preventDefault();const value=q('#plugin-spec').value.trim();if(!value){showStatus('请输入插件包名、GitHub 地址或本机路径',true);return}runButton(q('#save-plugin'),'正在添加...',()=>body(fetch('/admin/public-plugins',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'add',spec:value})})).then(async()=>{q('#plugin-dialog').close();showStatus('已添加公共插件：'+value+'。已运行 Runtime 请重启。');await loadPlugins()})).catch(error=>showStatus(error.message,true))};q('#install-plugins').onclick=()=>runButton(q('#install-plugins'),'正在校验...',()=>body(fetch('/admin/public-plugins',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'install'})})).then(async()=>{showStatus('公共插件 Profile 已安装并校验通过。');await loadPlugins()})).catch(error=>showStatus(error.message,true))
`
  const sessionScript = String.raw`function rpcValue(data){if(data?.result?.ok===false)throw new Error(data.result.error?.message||'Runtime 返回失败');if(data?.result?.ok!==true)throw new Error('Runtime 返回了无法识别的结果');return data.result.value}
function eventText(event){const data=event?.data;if(typeof data==='string')return data;if(data&&typeof data==='object'){if(typeof data.text==='string')return data.text;if(typeof data.content==='string')return data.content;if(Array.isArray(data.content))return data.content.map(part=>typeof part==='string'?part:part?.text||JSON.stringify(part)).join('\n')}return JSON.stringify(data,null,2)}
function renderHistory(value){const records=Array.isArray(value?.records)?value.records:[];sessions.textContent=records.length===0?'该会话暂无可显示的历史记录。':records.map(record=>{const event=record?.event||record,time=typeof event?.time==='number'?new Date(event.time).toLocaleString('zh-CN'):'';return '['+time+'] '+(event?.type||record?.type||'未知事件')+'\n'+eventText(event)}).join('\n\n');sessions.hidden=false;sessionDetailActions.replaceChildren(Object.assign(document.createElement('button'),{textContent:'关闭会话详情',onclick:()=>{sessions.hidden=true;sessionDetailActions.replaceChildren()}}));sessions.scrollIntoView({behavior:'smooth',block:'start'})}
async function readSessions(userId,method,payload){const data=await body(fetch('/admin/sessions/'+encodeURIComponent(userId)+'/'+method,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)})),value=rpcValue(data);if(method==='list'){const items=Array.isArray(value.items)?value.items:[];sessionDetailActions.replaceChildren();sessions.hidden=true;if(items.length===0){sessionActions.replaceChildren(emptyResource('该用户没有已保存的会话。'));return}sessionActions.replaceChildren(...items.map(item=>{const row=document.createElement('div'),name=document.createElement('span'),meta=document.createElement('span'),open=document.createElement('button'),sessionId=item.sessionId||item.id,throughSeq=Number.isSafeInteger(item.projections?.asOfSeq)?item.projections.asOfSeq:(item.blank?-1:undefined);row.className='session-row';name.className='session-name';meta.className='resource-meta';name.textContent=item.displayTitle||item.title||('会话 '+String(sessionId).slice(-8));meta.textContent=typeof item.updatedAt==='number'?new Date(item.updatedAt).toLocaleString('zh-CN'):String(sessionId);open.textContent='查看详情';open.onclick=()=>{if(!Number.isSafeInteger(throughSeq)){showStatus('该会话缺少历史游标，无法读取详情。请先在普通用户页面打开一次该会话。',true);return}runButton(open,'正在读取...',()=>readSessions(userId,'history',{sessionId,throughSeq,maxMessages:100})).catch(error=>showStatus(error.message,true))};row.append(name,meta,open);return row}));return}renderHistory(value)}
`
  const mcpScript = String.raw`let publicMcp=[]
function mcpSecretObject(names){return Object.fromEntries((names||[]).map(name=>[name,'']))}
function parseMcpDictionary(id){const text=q('#'+id).value.trim();if(!text)return{};let value;try{value=JSON.parse(text)}catch{throw new Error('JSON 配置格式无效')}if(!value||Array.isArray(value)||typeof value!=='object'||Object.values(value).some(item=>typeof item!=='string'))throw new Error('JSON 配置必须是字符串字典');return value}
function mcpPayload(server,enabled=server.enabled){return server.transport==='stdio'?{name:server.name,enabled,transport:server.transport,command:server.command,args:server.args,cwd:server.cwd,env:mcpSecretObject(server.envKeys),toolCallTimeoutMs:server.toolCallTimeoutMs,failOnStartupError:server.failOnStartupError}:{name:server.name,enabled,transport:server.transport,url:server.url,headers:mcpSecretObject(server.headerNames),toolCallTimeoutMs:server.toolCallTimeoutMs,failOnStartupError:server.failOnStartupError}}
function toggleMcpTransport(){const stdio=q('#mcp-transport').value==='stdio';q('#mcp-stdio-fields').hidden=!stdio;q('#mcp-http-fields').hidden=stdio}
function clearMcpForm(){q('#mcp-dialog-title').textContent='添加公共 MCP';q('#mcp-name').disabled=false;q('#mcp-name').value='';q('#mcp-transport').value='streamable-http';q('#mcp-enabled').checked=true;q('#mcp-timeout').value='60000';q('#mcp-startup').checked=false;q('#mcp-command').value='';q('#mcp-args').value='';q('#mcp-cwd').value='';q('#mcp-env').value='{}';q('#mcp-url').value='';q('#mcp-headers').value='{}';toggleMcpTransport()}
function openMcp(server){clearMcpForm();if(server){q('#mcp-dialog-title').textContent='修改公共 MCP';q('#mcp-name').value=server.name;q('#mcp-name').disabled=true;q('#mcp-transport').value=server.transport;q('#mcp-enabled').checked=server.enabled;q('#mcp-timeout').value=String(server.toolCallTimeoutMs);q('#mcp-startup').checked=server.failOnStartupError;if(server.transport==='stdio'){q('#mcp-command').value=server.command;q('#mcp-args').value=server.args.join('\n');q('#mcp-cwd').value=server.cwd;q('#mcp-env').value=JSON.stringify(mcpSecretObject(server.envKeys),null,2)}else{q('#mcp-url').value=server.url;q('#mcp-headers').value=JSON.stringify(mcpSecretObject(server.headerNames),null,2)}}toggleMcpTransport();q('#mcp-dialog').showModal()}
function renderMcp(){const root=q('#mcp-list');if(publicMcp.length===0){root.replaceChildren(emptyResource('暂无公共 MCP'));return}root.replaceChildren(...publicMcp.map(server=>{const row=document.createElement('div'),info=document.createElement('div'),title=document.createElement('p'),meta=document.createElement('p'),actions=document.createElement('div'),edit=document.createElement('button'),toggle=document.createElement('button'),remove=document.createElement('button');row.className='resource-row';title.className='resource-name';meta.className='resource-meta';actions.className='resource-actions';title.textContent=server.name;meta.textContent=server.serverName+' · '+(server.transport==='stdio'?'stdio · '+server.command:'Streamable HTTP · '+server.url)+' · '+(server.enabled?'已启用':'已停用');edit.textContent='修改';edit.onclick=()=>openMcp(server);toggle.textContent=server.enabled?'停用':'启用';toggle.onclick=()=>runButton(toggle,server.enabled?'正在停用...':'正在启用...',()=>body(fetch('/admin/public-mcp',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(mcpPayload(server,!server.enabled))})).then(data=>{publicMcp=data.servers;renderMcp();showStatus('公共 MCP 已更新，现有 Runtime 已停止。')})).catch(error=>showStatus(error.message,true));remove.className='danger-button';remove.textContent='删除';remove.onclick=()=>{if(!confirm('删除公共 MCP「'+server.name+'」？'))return;body(fetch('/admin/public-mcp/'+encodeURIComponent(server.name),{method:'DELETE'})).then(data=>{publicMcp=data.servers;renderMcp();showStatus('公共 MCP 已删除，现有 Runtime 已停止。')}).catch(error=>showStatus(error.message,true))};info.append(title,meta);actions.append(edit,toggle,remove);row.append(info,actions);return row}))}
async function loadMcp(){const data=await body(fetch('/admin/public-mcp'));publicMcp=data.servers;renderMcp()}
q('#open-mcp').onclick=()=>openMcp();q('#mcp-transport').onchange=toggleMcpTransport;q('#save-mcp').onclick=event=>{event.preventDefault();const transport=q('#mcp-transport').value,name=q('#mcp-name').value.trim(),common={name,enabled:q('#mcp-enabled').checked,transport,toolCallTimeoutMs:Number(q('#mcp-timeout').value),failOnStartupError:q('#mcp-startup').checked};let payload;try{payload=transport==='stdio'?{...common,command:q('#mcp-command').value.trim(),args:q('#mcp-args').value.split('\n').map(value=>value.trim()).filter(Boolean),cwd:q('#mcp-cwd').value.trim(),env:parseMcpDictionary('mcp-env')}:{...common,url:q('#mcp-url').value.trim(),headers:parseMcpDictionary('mcp-headers')}}catch(error){showStatus(error.message,true);return}runButton(q('#save-mcp'),'正在保存...',()=>body(fetch('/admin/public-mcp',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(payload)})).then(data=>{publicMcp=data.servers;q('#mcp-dialog').close();renderMcp();showStatus('公共 MCP 已保存，现有 Runtime 已停止。')})).catch(error=>showStatus(error.message,true))}
`
  return html
    .replace(/async function readModel\(\)\{.*?async function load\(/su, `${modelScript}async function load(`)
    .replace(/<div class="panel config-grid"><div>.*?<div id="model-state"/su, '<div class="panel"><div id="model-state"')
    .replace('</div></div></section><section id="skills"', '</div></section><section id="skills"')
    .replace(/<h2>添加自定义提供商<\/h2>/u, '<h2 id="provider-dialog-title">添加提供商</h2>')
    .replace(/<select id="provider-kind"><option value="custom">自定义提供商<\/option><\/select>/u, '<select id="provider-kind"><option value="deepseek">DeepSeek 提供商</option><option value="custom">自定义提供商</option></select>')
    .replace('<label class="field">提供商 ID', '<label id="provider-id-field" class="field">提供商 ID')
    .replace('<label class="field">显示名称', '<label id="provider-name-field" class="field">显示名称')
    .replace('<label class="field">协议', '<label id="provider-api-field" class="field">协议')
    .replace('<label class="field">模型 ID', '<label id="provider-model-field" class="field">模型 ID')
    .replace("q('#save-provider').textContent='更新提供商';dialog.showModal()", "q('#provider-kind').value=provider.builtIn?'deepseek':'custom';toggleProviderFields();q('#save-provider').textContent='更新提供商';dialog.showModal()")
    .replace("q('#open-provider').onclick=()=>{clearProviderForm();q('#save-provider').textContent='保存提供商';dialog.showModal()}", "q('#open-provider').onclick=()=>{clearProviderForm();q('#provider-kind').value='deepseek';toggleProviderFields();q('#save-provider').textContent='保存提供商';dialog.showModal()}")
    .replace("q('#save-model').onclick=()=>saveModel().catch(error=>showStatus(error.message,true))", "q('#provider-kind').onchange=toggleProviderFields;q('#save-model')?.addEventListener('click',()=>saveModel().catch(error=>showStatus(error.message,true)))")
    .replace("if(provider.builtIn){q('#base-url').focus();showStatus('内置 DeepSeek 的地址请在左侧修改，API Key 可直接重新填写。');return}", "if(provider.builtIn){clearProviderForm();q('#provider-kind').value='deepseek';q('#provider-url').value=provider.baseURL;toggleProviderFields();q('#save-provider').textContent='修改 DeepSeek';dialog.showModal();return}")
    .replace('</style>', '.nav a{white-space:nowrap}.section-heading{display:flex;justify-content:space-between;align-items:center;gap:16px}.section-heading h2{margin:0}.section-heading .resource-actions{display:flex;gap:8px}textarea{width:100%;resize:vertical;color:var(--text);background:#090a09;border:1px solid var(--line);padding:10px;font:12px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace;outline:0}.checkbox-field{display:flex;align-items:center;gap:9px;color:var(--muted);margin:10px 0}.checkbox-field input{width:auto;margin:0}.mcp-grid{display:grid;grid-template-columns:1fr 1fr;gap:0 18px}.mcp-grid .wide{grid-column:1/-1}#sessions{border:1px solid var(--line);background:#090a09;color:var(--text);padding:16px;white-space:pre-wrap;overflow-wrap:anywhere;max-height:65vh;overflow:auto}.session-row{align-items:center}.session-row .resource-meta{white-space:nowrap}@media(max-width:800px){.topbar span{display:block}.topbar-status{margin-top:6px}.section-heading{align-items:flex-start}.session-row{flex-wrap:wrap}.session-row .resource-meta{width:100%}.mcp-grid{grid-template-columns:1fr}}</style>')
    .replace('<a href="#skills">公共 Skill</a>', '<a href="#mcp">公共 MCP</a><a href="#skills">公共 Skill</a>')
    .replace('<section id="skills">', '<section id="mcp"><div class="section-heading"><h2>公共 MCP</h2><button id="open-mcp" class="primary">添加 MCP</button></div><div id="mcp-list" class="resource-list"></div></section><section id="skills">')
    .replace('<section id="skills"><h2>公共 Skill</h2><div id="skills-list" class="resource-list"></div></section>', '<section id="skills"><div class="section-heading"><h2>公共 Skill</h2><button id="open-skill" class="primary">添加 Skill</button></div><p class="section-note">可快速创建 SKILL.md，也可导入包含 scripts、references、assets 等文件的完整目录。</p><div id="skills-list" class="resource-list"></div></section>')
    .replace('<section id="plugins"><h2>公共插件</h2><div id="plugins-list" class="resource-list"></div></section>', '<section id="plugins"><div class="section-heading"><h2>公共插件</h2><div class="resource-actions"><button id="install-plugins">安装并校验 Profile</button><button id="open-plugin" class="primary">添加插件</button></div></div><p class="section-note">通过 DSH 原生 Profile 命令管理，普通用户下次启动 Runtime 时使用更新后的 Profile。</p><div id="plugins-list" class="resource-list"></div></section>')
    .replace('<dialog id="provider-dialog">', '<dialog id="skill-dialog"><form method="dialog"><h2 id="skill-dialog-title">添加公共 Skill</h2><label class="field">添加方式<select id="skill-mode"><option value="manual">快速创建 SKILL.md</option><option value="folder">导入完整 Skill 文件夹</option></select></label><div id="skill-manual-fields"><label class="field">Skill 名称<input id="skill-name" placeholder="例如 release-check"></label><label class="field">SKILL.md 内容<textarea id="skill-content" rows="10"></textarea></label></div><div id="skill-folder-fields" hidden><label class="field">Skill 文件夹<input id="skill-folder" type="file" webkitdirectory multiple></label></div><div class="dialog-actions"><button value="cancel">取消</button><button id="save-skill" value="default" class="primary">保存 Skill</button></div></form></dialog><dialog id="plugin-dialog"><form method="dialog"><h2>添加公共插件</h2><label class="field">插件来源<input id="plugin-spec" placeholder="插件包名、GitHub 地址或本机路径"></label><div class="dialog-actions"><button value="cancel">取消</button><button id="save-plugin" value="default" class="primary">添加插件</button></div></form></dialog><dialog id="provider-dialog">')
    .replace('<dialog id="provider-dialog">', '<dialog id="mcp-dialog"><form method="dialog"><h2 id="mcp-dialog-title">添加公共 MCP</h2><div class="mcp-grid"><label class="field">公共名称<input id="mcp-name" placeholder="例如 ops"></label><label class="field">传输方式<select id="mcp-transport"><option value="streamable-http">Streamable HTTP</option><option value="stdio">stdio</option></select></label><label class="field">调用超时（毫秒）<input id="mcp-timeout" type="number" min="1" max="3600000" step="1" value="60000"></label><div><label class="checkbox-field"><input id="mcp-enabled" type="checkbox" checked>启用</label><label class="checkbox-field"><input id="mcp-startup" type="checkbox">连接失败时阻止 Runtime 启动</label></div><div id="mcp-http-fields" class="wide"><label class="field">MCP 地址<input id="mcp-url" type="url" placeholder="https://mcp.example.com/mcp"></label><label class="field">请求标头（JSON）<textarea id="mcp-headers" rows="5">{}</textarea></label></div><div id="mcp-stdio-fields" class="wide" hidden><label class="field">命令<input id="mcp-command" placeholder="mcp-server"></label><label class="field">参数（每行一个）<textarea id="mcp-args" rows="4"></textarea></label><label class="field">工作目录<input id="mcp-cwd"></label><label class="field">环境变量（JSON）<textarea id="mcp-env" rows="5">{}</textarea></label></div></div><div class="dialog-actions"><button value="cancel">取消</button><button id="save-mcp" value="default" class="primary">保存 MCP</button></div></form></dialog><dialog id="provider-dialog">')
    .replace(/async function readSessions\(userId,method,payload\)\{.*?\}\nasync function loadSkills/su, `${sessionScript}async function loadSkills`)
    .replace(/async function loadSkills\(\)\{.*?\}\nq\('#add-user'\)/su, `${resourceScript}q('#add-user')`)
    .replace("q('#add-user')", `${mcpScript}q('#add-user')`)
    .replace('Promise.all([load(),readModel(),loadSkills(),loadPlugins()])', 'Promise.all([load(),readModel(),loadMcp(),loadSkills(),loadPlugins()])')
    .replace('</nav></aside>', '</nav><button id="admin-logout">退出管理员登录</button></aside>')
    .replace("q('#add-user')", "q('#admin-logout').onclick=()=>fetch('/admin/auth/logout',{method:'POST'}).then(()=>location.href='/admin/login');q('#add-user')")
}
